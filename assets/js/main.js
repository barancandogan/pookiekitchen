'use strict';

/**
 * Intentionally empty.
 *
 * The site ships one theme and every page is complete server-side, so there is
 * nothing for a script to enhance. The file exists because build.js copies
 * assets/ verbatim and layout.js references it — and because the moment
 * something does need scripting (a dark-mode toggle, an order widget), this is
 * where it goes, already wired up and deferred.
 */
